# Wrangler configuration for Keyper - Cloudflare Pages deployment
# Made with ❤️ by Pink Pixel

name = "keyper"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# Environment variables (set these in Cloudflare dashboard)
# VITE_SUPABASE_URL = "your-supabase-url"
# VITE_SUPABASE_ANON_KEY = "your-supabase-anon-key"

# Note: Headers, redirects, and routing are handled by:
# - _headers file for security headers and caching
# - _redirects file for SPA routing
# - _routes.json file for Cloudflare Pages routing
